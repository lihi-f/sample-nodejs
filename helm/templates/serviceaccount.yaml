{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "sample-nodejs.serviceAccountName" . }}
  labels:
    {{- include "sample-nodejs.labels" . | nindent 4 }}
{{- end }}
